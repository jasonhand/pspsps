{
  "name": "pspsps",
  "version": "1.0.0",
  "description": "Pet finder web application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "netlify dev",
    "build": "cd netlify/functions && npm install"
  },
  "dependencies": {
    "axios": "^1.6.5",
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.10.0",
    "@netlify/plugin-functions-install-core": "^1.0.8"
  }
}