{
  "name": "pspsps",
  "version": "1.0.0",
  "description": "Pet finder web application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "netlify dev"
  },
  "dependencies": {
    "axios": "^1.6.5"
  },
  "devDependencies": {
    "netlify-cli": "^17.10.0"
  }
}